def main() -> int64 {
    image img = load_image("img/bench.jpg")

    for (i = 0 .. 50) {
        image out = gaussian_blur(img, 6)
        save_image(out, "bench_gaussian_blur_out.png")
    }

    return 0
}
